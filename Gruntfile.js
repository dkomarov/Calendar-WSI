module.exports = function(grunt){

  //Configuration
  grunt.initConfig({
    //pass in options to plugins, references to files etc.
    concat: {
      js: {

      }
    }
  });

  // Register tasks
  grunt.registerTask({})
}
